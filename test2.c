// // start when first process is ready to run
        // if (strcmp(arr_process[0].currentState,"READY")){
        //     strcpy(arr_process[i].state,"RUNNING");
        // }

        // // state changes from NEW to READY when clock hits Arrival Time
        // // state changes from 
        
        // for(i=0;i<numCommands;i++)
        // {
        //     // check if the process is ready
        //     if(clock >= arr_process[i].arrivalTime && strcmp(arr_process[i].current state,"NEW")==0){
        //         strcpy(arr_process[i].current state,"READY");
        //     }

        //     // check if process is complete
        //     if(arr_process[i].cpuCounter==arr_process[i].cpuTime){
        //         strcpy(arr_process[i].current state,"TERMINATED");
        //     }
        //     // check if process needs to be in io
        //     // check if proces needs to go back to ready
        //     // check ready to running
        // }